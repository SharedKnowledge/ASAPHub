package net.sharksystem.hub;

import net.sharksystem.utils.Log;

import java.io.InputStream;
import java.io.OutputStream;

public class Helper {
    public static void syncStream(InputStream is, OutputStream os, long waitInMillis) {
        Log.writeLog(Helper.class,"TODO: implement a very small protocol to read remaining bytes from stream");
    }
}
