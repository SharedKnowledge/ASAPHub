## Add ASAPJava dependency using a local maven repository 

pre-install the asapjava library using this command:

`mvn org.apache.maven.plugins:maven-install-plugin:2.5.1:install-file 
-Dfile=libs/ASAPJava.jar
`